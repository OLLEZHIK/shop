# Задача: собрать данные о 10 ветеринарных клиниках Братиславы

**Роль:** Data agent.
**Тип:** тестовая задача (проверка процесса), объём намеренно маленький.
**Ветка:** `antigravity/data-vet-clinics-bratislava`

## Что сделать

Найти 10 ветеринарных клиник, работающих в Братиславе, и собрать по каждой
базовые публичные данные.

## Источники

- Только сайты самих клиник, публичные каталоги/справочники (например,
  Zoznam.sk, Azet.sk) и прайс-листы, которые сами клиники публикуют.
- Не использовать данные Google Maps / Google Places API как источник
  (искать через поиск можно, но не копировать структурированные данные
  из карт как есть — только то, что подтверждается сайтом самой клиники).
- Не копировать чужие отзывы.
- Не выдумывать ничего. Если поле не удалось найти — оставить пустым и
  написать причину в `notes`.

## Формат результата

Создать файл `data/vet-clinics-bratislava.csv` со столбцами:

```
name,address,district,phone,email,website,source_url,observed_at,notes
```

- `district` — район Братиславы, если удалось определить (см. список
  17 районов в `docs/concept.md`, если файл уже есть в репозитории;
  если файла нет — не изобретать район, оставить пустым).
- `source_url` — ссылка, откуда взяты данные по этой строке.
- `observed_at` — дата в формате `YYYY-MM-DD`, когда данные проверялись.
- Один клиника = одна строка.

## Критерии готовности (Definition of Done)

- В файле ровно 10 строк с данными (не считая заголовка).
- У каждой строки заполнены минимум: `name`, `address` или `phone`,
  `source_url`, `observed_at`.
- Телефон и сайт клиники по возможности проверены (страница открывается,
  номер выглядит правдоподобно для Словакии).
- Нет дублей (одна и та же клиника дважды).
- Изменения закоммичены в ветку `antigravity/data-vet-clinics-bratislava`
  с понятным сообщением коммита.
- Открыт Pull Request в `main` с коротким описанием: сколько клиник
  собрано, откуда, что не удалось найти.

## Если что-то неясно

Не додумывать. Написать вопрос или ограничение прямо в описании PR и
остановиться на этом месте.
